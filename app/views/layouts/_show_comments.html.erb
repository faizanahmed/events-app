<br>
<br>
<% @comments.each do |comment| %>
<ul class="list-group">

<%u=User.find(comment.user_id)%>
<a   class="list-group-item active">
 <%= u.name %> said :</a>
<a  class="list-group-item">
<%= comment.content %>
</a>

<%= link_to "DELETE" ,event_comment_path(@event,comment), method: :delete ,class: "btn btn-default btn-danger"%>
<br>
</ul>
<% end %>