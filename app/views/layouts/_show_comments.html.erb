<% @comments.each do |comment| %>
<ul class="list-group">

<%u=User.find(comment.user_id)%>
<li class="list-group-item active">
Commenter: <%= u.name %></li>
<li class="list-group-item">

Content:
<%= comment.content %>
</li>
<%= link_to "DELETE" ,event_comment_path(@event,comment), method: :delete ,class: "btn btn-primary"%>
<br>
<% end %>